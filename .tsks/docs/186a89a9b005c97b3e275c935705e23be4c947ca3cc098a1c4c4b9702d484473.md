Phase 2: Add priority field with values: low, medium, high, critical

## Phase 2 – Beta

**Goal:** Expand the feature set to cover richer metadata, improve developer experience, and harden quality via integration tests.

**Technical milestones:**
- Integration test suite covers all commands with ≥ 80% path coverage
- `tssk edit <id>` command implemented and tested
- Shell completions generated and documented

**Success metrics:**
- Integration test pass rate: 100%
- `go test ./...` runs in < 30 s on CI
- No high/critical CVEs in dependency tree

**Review point:** 2026-07-31

## Files to Modify/Create

### Modify
- `internal/task/task.go` — Add `Priority` field to Task struct with type (e.g., `type Priority string`) and validation; may affect `DocHash` if considered immutable
- `internal/store/store.go` — Update `Add()` to accept priority; add `UpdatePriority()` method; adjust JSONL serialization
- `cmd/add.go` — Add `--priority` flag
- `cmd/edit.go` — Add `--priority` flag (once edit command exists)
- `cmd/list.go` — Optionally add `--priority` filter
- `cmd/show.go` — Display priority in output
- `README.md` — Document priority field and usage

### Create
- Unit tests for priority field in `internal/task/task_test.go`
- Unit tests for priority in store operations
- Possibly a new `internal/task/priority.go` if priority logic grows complex