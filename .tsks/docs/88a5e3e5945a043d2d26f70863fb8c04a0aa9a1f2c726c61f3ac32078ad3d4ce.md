Phase 1: Integrate SonarCloud for code quality scanning - Issue #6

## Phase 1 – MVP

**Goal:** A polished, documented, and discoverable MVP suitable for daily use by individual developers and small AI-agent workflows.

**Technical milestones:**
- `tssk list` supports `--status`, `--title` filter flags
- All commands support `--json` for scripting/agent consumption
- README includes a quick-start guide, command reference, and data format description

**Success metrics:**
- Zero open P0/P1 bugs
- `tssk --help` and per-command help text cover all flags
- README score ≥ 8/10 (peer review checklist)

## Files to Modify/Create

### Modify
- `.github/workflows/build.yml` — Add SonarCloud scan step (currently uses SonarQube; may need migration)
- `sonar-project.properties` — Update for SonarCloud compatibility (project key, organization, token refs)
- `.github/workflows/ci.yml` — May need coverage output format adjustments for SonarCloud

### Create
- `.github/workflows/sonarcloud.yml` — Dedicated SonarCloud scan workflow if not embedded in build.yml
- GitHub secret `SONAR_TOKEN` (not a file, but configuration needed)