Identify and eliminate duplicated code across the codebase to improve maintainability and reduce bugs.

Tasks:
- Run code duplication detection tools (e.g., cpd, duplo, sonar)
- Identify duplicate code blocks (>3 lines)
- Extract common logic into reusable functions/utilities
- Replace duplicates with function calls
- Update tests to cover refactored code
- Verify no functionality changes

Acceptance Criteria:
- Duplication reduced by at least 50%
- All existing tests pass
- No new bugs introduced

AI-Friendly: Pattern matching for duplicate detection, systematic refactoring

## Files to Modify/Create

### Modify
- Files identified by code duplication analysis (run `golangci-lint run --enable=dupl` or similar tool)
- Likely candidates based on codebase structure:
  - `internal/store/config.go` — Env var parsing duplication
  - `cmd/*.go` — Possible duplicated store opening patterns, error handling
  - `internal/store/local.go` and `internal/store/s3.go` — Possible similar read/write patterns

### Create
- `internal/store/helpers.go` — Extracted utility functions if duplication spans multiple files
- Additional unit tests for refactored code in relevant `*_test.go` files