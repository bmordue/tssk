Implement detail file reading from database (or keep as file-based):

Files to modify:
- internal/store/db.go - Add ReadDetail method

Decision needed: Store detail files in database or keep as files?
Option A: Store detail content in details table (BLOB/TEXT)
Option B: Keep detail files on filesystem (reuse LocalBackend logic)

Implement based on decision:
- If Option A: Query details table by doc_hash
- If Option B: Reuse file reading logic from LocalBackend