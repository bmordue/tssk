Phase 2: Generate and document shell completion scripts

## Phase 2 – Beta

**Goal:** Expand the feature set to cover richer metadata, improve developer experience, and harden quality via integration tests.

**Technical milestones:**
- Integration test suite covers all commands with ≥ 80% path coverage
- `tssk edit <id>` command implemented and tested
- Shell completions generated and documented

**Success metrics:**
- Integration test pass rate: 100%
- `go test ./...` runs in < 30 s on CI
- No high/critical CVEs in dependency tree

**Review point:** 2026-07-31

## Files to Modify/Create

### Modify
- `cmd/root.go` — Add a `completion` subcommand that generates scripts for bash/zsh/fish via Cobra's built-in completion support
- `README.md` — Document how to install and enable shell completions
- `Makefile` or `.github/workflows/release.yml` — Optionally include completion generation in build process

### Create
- `cmd/completion.go` — Cobra command for `tssk completion <bash|zsh|fish>`
- Shell completion script files (optional, generated on-the-fly):
  - `completions/tssk.bash`
  - `completions/tssk.zsh`
  - `completions/tssk.fish`