Select an appropriate embedded database for tssk (e.g., SQLite via mattn/go-sqlite3, or LiteFS, or similar).
Considerations:
- Must be embeddable (no external server)
- Cross-platform compatibility
- Transaction support for atomic operations
- Migration support
- Community support and maintenance

Add the chosen database driver to go.mod.