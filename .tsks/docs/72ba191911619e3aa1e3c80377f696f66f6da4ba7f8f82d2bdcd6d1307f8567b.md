Implement detail file deletion from database (or keep as file-based):

Files to modify:
- internal/store/db.go - Add DeleteDetail method

Implement based on ReadDetail decision:
- If Option A (details in DB): DELETE from details table
- If Option B (details as files): Reuse file deletion logic from LocalBackend

Ensure:
- Handle non-existent detail gracefully
- Error handling