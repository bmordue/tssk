Allow me to add tags, for example to differentiate low-level tasks from epics, features etc

## Files to Modify/Create

### Modify
- `internal/task/task.go` — Tags field already exists; may need validation or type system for tag categories
- `internal/store/store.go` — Tag methods likely exist; may need enhancements for tag queries
- `cmd/add.go` — Add `--tags` flag (may already exist via `tags.go`)
- `cmd/list.go` — Add `--tag` filter flag for filtering by specific tags
- `cmd/tags.go` — Already exists with add/remove/list subcommands; may need enhancements
- `README.md` — Document tag usage, conventions for epics/features/etc.

### Create
- Integration tests for tag filtering
- Documentation in `docs/tags.md` for tag conventions and best practices