Add --json flag to tssk show command

## Files to Modify/Create

### Modify
- `cmd/show.go` — Add `--json` flag; when present, output task as JSON object instead of formatted text
- `cmd/json.go` — Already has `printJSON()` helper; reuse for show output
- `README.md` — Document the `--json` flag for `tssk show`

### Create
- Unit tests for JSON output in `cmd/show_test.go` (if new file) or extend existing tests
