Phase 4: Aggregate tasks across multiple repositories (Q2 2027)

## Phase 4 – Future / Post-v1.0

These items are aspirational and subject to revision at the v1.0 review.

## Files to Modify/Create

### Modify
- `internal/store/multistore.go` — Extend to support remote repository aggregation, not just local collections
- `internal/store/config.go` — Add multi-repo configuration (repository URLs, auth, sync settings)
- `cmd/list.go` — Add flags for cross-repo filtering
- `cmd/root.go` — May need `tssk aggregate` or `tssk repos` subcommands

### Create
- `internal/store/remote.go` — Remote repository backend (git clone, fetch, parse)
- `internal/store/remote_test.go` — Tests for remote repository handling
- `cmd/repos.go` — Manage registered repositories (`add`, `remove`, `list`)
- `internal/aggregate/` — Aggregation logic package
  - `internal/aggregate/aggregate.go` — Cross-repo task aggregation, deduplication
  - `internal/aggregate/aggregate_test.go` — Tests
- Documentation in `docs/multi-repo.md`