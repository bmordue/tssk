Phase 1: Add filtering flags to list command for status and title search

## Phase 1 – MVP

**Goal:** A polished, documented, and discoverable MVP suitable for daily use by individual developers and small AI-agent workflows.

**Technical milestones:**
- `tssk list` supports `--status`, `--title` filter flags
- All commands support `--json` for scripting/agent consumption
- README includes a quick-start guide, command reference, and data format description

**Success metrics:**
- Zero open P0/P1 bugs
- `tssk --help` and per-command help text cover all flags
- README score ≥ 8/10 (peer review checklist)

## Files to Modify/Create

### Modify
- `cmd/list.go` — Add `--status` and `--title` flags, implement filtering logic
- `cmd/root.go` — Update help text if flags change usage
- `internal/store/store.go` — May need filtering helper methods (or filter in cmd layer)
- `README.md` — Document new `--status` and `--title` flags

### Create
- Unit tests for filtering logic (likely in `cmd/list_test.go` if new file, or extend existing test patterns)