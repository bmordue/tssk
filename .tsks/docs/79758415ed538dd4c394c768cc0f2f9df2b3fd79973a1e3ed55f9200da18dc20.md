Risk R-7: Build pipeline (CI/CD) is fragile or slow. Mitigation: Address in Phase 0 (#8); add caching and parallelism

## Risk Details

- **Likelihood:** Medium
- **Impact:** Medium
- **Mitigation:** Address in Phase 0 (#8); add caching and parallelism

## Files to Modify/Create

### Modify
- `.github/workflows/ci.yml` — Add Go module caching, parallelize lint/test jobs
- `.github/workflows/build.yml` — Add caching for Go build artifacts, optimize SonarQube step
- `.github/workflows/security.yml` — Ensure govulncheck caching where possible
- `.github/workflows/health-check.yml` — Optimize matrix builds

### Create
- No new workflow files necessarily needed; existing workflows should be optimized
- `.github/dependabot.yml` — Optional: automated dependency updates to keep CI healthy