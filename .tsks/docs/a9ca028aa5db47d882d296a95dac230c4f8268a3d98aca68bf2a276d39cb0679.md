Phase 0: Set up GitHub Actions with security scanning, linting, and quality gates - Issue #8

## Phase 0 – Foundation

**Goal:** Stabilise the core task model, JSONL store, CLI skeleton, tests, and CI/CD pipeline before feature development begins.

**Success criteria:**
- All existing tests pass on every commit via CI
- Automated security scanning runs on each pull request

## Files to Modify/Create

### Modify
- `.github/workflows/ci.yml` — Add caching for Go modules, parallelize jobs, optimize build matrix
- `.github/workflows/build.yml` — Ensure build pipeline is robust and fast
- `.github/workflows/security.yml` — Verify govulncheck runs on schedule and PRs
- `.golangci.yml` — Tune linter rules, add new linters as needed
- `go.mod` / `go.sum` — Review and update dependencies, add security-related deps if needed

### Create
- `.github/workflows/` — No new workflows needed; existing ones cover the requirements