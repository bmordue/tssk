Implement the ReadTasksData method for DatabaseBackend:

Files to modify:
- internal/store/db.go - Add ReadTasksData method

Implement:
- Query all tasks from database with proper ordering
- Join dependencies and tags tables
- Serialize to JSON format (maintaining backward compatibility with JSONL format expected by Store)
- Handle empty database case (return nil, nil)
- Error handling with proper error messages