Phase 4: Allow pre/post hooks on task operations for custom automation (Q4 2026)

## Phase 4 – Future / Post-v1.0

These items are aspirational and subject to revision at the v1.0 review.

## Files to Modify/Create

### Modify
- `internal/store/store.go` — Add hook invocation points around `Add()`, `UpdateStatus()`, `UpdateDetail()`, etc.
- `internal/store/config.go` — Add hook configuration fields (paths to hook scripts, enable/disable flags)
- `cmd/root.go` — Possibly add `tssk hook` subcommand for managing hooks

### Create
- `internal/hook/` — New package for hook system
  - `internal/hook/hook.go` — Hook type definitions, registration, execution logic
  - `internal/hook/hook_test.go` — Unit tests for hook execution
- `.tssk/hooks/` — Default directory for hook scripts (e.g., `pre-add`, `post-status`)
- Example hook scripts in `hooks/examples/`
- Documentation in `docs/hooks.md`