Phase 2: Add tag/label metadata to tasks for categorization

## Phase 2 – Beta

**Goal:** Expand the feature set to cover richer metadata, improve developer experience, and harden quality via integration tests.

**Technical milestones:**
- Integration test suite covers all commands with ≥ 80% path coverage
- `tssk edit <id>` command implemented and tested
- Shell completions generated and documented

**Success metrics:**
- Integration test pass rate: 100%
- `go test ./...` runs in < 30 s on CI
- No high/critical CVEs in dependency tree

**Review point:** 2026-07-31

## Files to Modify/Create

### Modify
- `internal/task/task.go` — Tag methods already exist (`AddTag`, `RemoveTag`, `HasTag`); may need enhancements for tag metadata
- `internal/store/store.go` — Tag management methods may need extension
- `cmd/tags.go` — Already exists; may need enhancements for listing/filtering by tags
- `cmd/list.go` — Add `--tag` filter support if not already present
- `README.md` — Document tag/label usage and commands

### Create
- Integration tests for tag operations
- Possibly `cmd/tags_test.go` if not covered