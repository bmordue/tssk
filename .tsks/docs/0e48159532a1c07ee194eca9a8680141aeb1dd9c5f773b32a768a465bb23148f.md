Implement detail file writing to database (or keep as file-based):

Files to modify:
- internal/store/db.go - Add WriteDetail method

Implement based on ReadDetail decision:
- If Option A (details in DB): INSERT/UPDATE details table
- If Option B (details as files): Reuse file writing logic from LocalBackend

Ensure:
- Atomic write semantics
- Error handling
- Directory creation if needed (for file-based approach)