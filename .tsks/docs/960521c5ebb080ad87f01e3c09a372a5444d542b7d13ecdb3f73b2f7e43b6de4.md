Design the SQL schema for storing task data:

Tables:
- tasks: id (TEXT PK), title (TEXT), status (TEXT), created_at (TIMESTAMP), doc_hash (TEXT)
- task_dependencies: task_id (TEXT), depends_on (TEXT), composite PK
- task_tags: task_id (TEXT), tag (TEXT), composite PK

Include:
- Proper indexes for efficient querying
- Foreign key constraints
- Migration strategy (consider using golang-migrate or similar)

Files to create/modify:
- internal/store/schema.sql (or migrations/)