Risk R-3: Security vulnerabilities in Go dependencies. Mitigation: Automated dependency audits in CI (Phase 0/1); regular govulncheck runs

## Risk Details

- **Likelihood:** Low
- **Impact:** High
- **Mitigation:** Automated dependency audits in CI (Phase 0/1); regular `govulncheck` runs

## Files to Modify/Create

### Modify
- `.github/workflows/security.yml` — Already exists; ensure govulncheck runs on every PR and on a weekly schedule
- `go.mod` / `go.sum` — Regularly update dependencies to patched versions

### Create
- No new files needed; existing security workflow covers the mitigation