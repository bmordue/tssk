Phase 4: Optional sync to remote store (Q4 2026)

## Phase 4 – Future / Post-v1.0

These items are aspirational and subject to revision at the v1.0 review.

## Files to Modify/Create

### Modify
- `internal/store/config.go` — Add remote sync configuration options (sync interval, conflict resolution strategy)
- `internal/store/s3.go` — Already exists; may need enhancements for bidirectional sync
- `cmd/root.go` — Add `tssk sync` command registration
- `.github/workflows/security.yml` — Ensure S3 credentials are handled securely in CI

### Create
- `internal/store/sync.go` — Sync logic: conflict detection, merge strategies, delta computation
- `internal/store/sync_test.go` — Unit tests for sync operations
- `cmd/sync.go` — `tssk sync` command with `--push`, `--pull`, `--status` flags
- `internal/store/backend.go` — May need sync-related methods added to the Backend interface
- Documentation in `docs/sync.md`