Phase 2: Comprehensive integration tests covering all commands with >=80% path coverage

## Phase 2 – Beta

**Goal:** Expand the feature set to cover richer metadata, improve developer experience, and harden quality via integration tests.

**Technical milestones:**
- Integration test suite covers all commands with ≥ 80% path coverage
- `tssk edit <id>` command implemented and tested
- Shell completions generated and documented

**Success metrics:**
- Integration test pass rate: 100%
- `go test ./...` runs in < 30 s on CI
- No high/critical CVEs in dependency tree

**Review point:** 2026-07-31

## Files to Modify/Create

### Modify
- Existing `*_test.go` files across `cmd/`, `internal/store/`, `internal/task/` — Extend coverage where gaps exist

### Create
- `integration/` or `tests/integration/` — Directory for end-to-end CLI tests
- `integration/integration_test.go` — Main integration test harness that builds binary, runs CLI commands, verifies output
- Test fixtures: sample `.tsks/tasks.jsonl` files, sample detail markdown files
- Possibly a `testhelper/` package with shared test utilities