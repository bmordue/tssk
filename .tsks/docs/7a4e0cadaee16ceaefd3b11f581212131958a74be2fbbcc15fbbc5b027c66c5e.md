There is significant code duplication between ConfigFromFileAndEnv and ConfigFromEnv when parsing environment variables. The logic for handling EnvTasksFile, EnvDocsDir, EnvHashLength, and all S3-related environment variables is repeated.

To improve maintainability and adhere to the DRY principle, this duplicated logic should be extracted into a private helper function. This helper could take a *Config object, apply the environment variable overrides, and be called from both ConfigFromEnv and ConfigFromFileAndEnv after initial setup.

Originally posted by @gemini-code-assist[bot] in https://github.com/bmordue/tssk/pull/21#discussion_r2991266500

## Files to Modify/Create

### Modify
- `internal/store/config.go` — Extract duplicated env var parsing logic into a private helper function (e.g., `applyEnvOverrides(cfg *Config)`)
- `internal/store/config_test.go` — Ensure tests still pass after refactoring; add tests for the new helper if needed

### Create
- No new files needed; refactoring is contained within existing config.go