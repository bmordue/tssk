Phase 0: Set up CLI framework with core commands: add, list, show, status, and deps

## Phase 0 – Foundation

**Goal:** Stabilise the core task model, JSONL store, CLI skeleton, tests, and CI/CD pipeline before feature development begins.

**Success criteria:**
- All existing tests pass on every commit via CI
- Automated security scanning runs on each pull request