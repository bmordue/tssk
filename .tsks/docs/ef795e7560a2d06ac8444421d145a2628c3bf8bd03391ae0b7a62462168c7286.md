Add configuration parsing for the database backend:

Files to modify:
- internal/store/config.go

Implement:
- Add 'db' as a valid backend option in backend validation
- Parse database-specific config options (e.g., db_path)
- Add env var support: TSSK_DB_PATH
- Add config file field: db_path
- Update NewFromConfig factory to create DatabaseBackend when backend='db'