Phase 3: Publish v1.0.0 tag with pre-built binaries for Linux/macOS/Windows

## Phase 3 – v1.0

**Goal:** Production-ready v1.0 release with a stable public API, comprehensive documentation, and distribution packaging.

**Technical milestones:**
- Semantic versioning (`v1.0.0`) tag published
- Pre-built binaries for Linux (amd64/arm64), macOS (amd64/arm64), and Windows (amd64)
- All public commands and flags documented in a versioned reference

**Success metrics:**
- Zero open P0 bugs at GA
- Documentation covers 100% of public commands
- Binary size < 15 MB
- Install-to-first-task time < 2 minutes (measured against documented quick-start)

## Files to Modify/Create

### Modify
- `.github/workflows/release.yml` — Ensure multi-arch binary builds are production-ready; add binary signing/notarization if needed
- `go.mod` — Set Go version for v1.0; ensure all dependencies are stable
- `README.md` — Update with v1.0 installation instructions pointing to pre-built binaries
- `CHANGELOG.md` — Add v1.0.0 release notes

### Create
- `CHANGELOG.md` — If not yet created, with comprehensive v1.0.0 release notes
- Release announcement in `docs/releases/v1.0.0.md`
- Code signing certificates or GPG keys for binary verification (setup, not files in repo)