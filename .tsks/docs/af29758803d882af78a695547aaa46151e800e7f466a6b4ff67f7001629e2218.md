Add a new command to move (default) or (optionally) delete the markdown details documents for tasks that have been marked as done or removed.

## Files to Modify/Create

### Modify
- `cmd/root.go` — Register new `tidy` (or `docs cleanup`) command in `init()`
- `internal/store/store.go` — Add `DeleteDetail(hash)` method if not already exposed; add method to find details for done/removed tasks
- `internal/store/backend.go` — `DeleteDetail` method already exists in interface

### Create
- `cmd/tidy.go` — New Cobra command with flags:
  - `--move` (default) — Move detail docs to an archive directory
  - `--delete` — Permanently delete detail docs
  - `--archive-dir` — Custom archive directory (default `.tsks/docs/archive/`)
- `.tsks/docs/archive/` — Default archive directory for moved documents
- Unit tests for tidy command
- Documentation in `README.md`