Command to set status 'removed'.
Should not show up in list output by default; add a flag to include ('--include-removed')

## Files to Modify/Create

### Modify
- `internal/task/task.go` — Add `removed` to the Status type's valid values and `IsValid()` method
- `cmd/status.go` — Allow `removed` as a valid status value
- `cmd/list.go` — Add `--include-removed` flag; filter out `removed` tasks by default
- `README.md` — Document the `removed` status and `--include-removed` flag

### Create
- Unit tests for removed status in `internal/task/task_test.go`
- Unit tests for `--include-removed` flag behavior