Implement the WriteTasksData method for DatabaseBackend:

Files to modify:
- internal/store/db.go - Add WriteTasksData method

Implement:
- Parse JSONL data into Task structs
- Use database transaction for atomic write
- DELETE all existing tasks, INSERT new tasks
- Handle dependencies and tags (delete + reinsert)
- Proper error handling and rollback on failure
- Maintain atomic write semantics (all-or-nothing)