Add an optional field to the JSONL for an effort estimate

## Files to Modify/Create

### Modify
- `internal/task/task.go` — Add `Estimate` field (e.g., `Estimate string` for values like "1h", "2d", or `EstimateMinutes *int`)
- `internal/store/store.go` — Update `Add()` to accept estimate parameter; add `UpdateEstimate()` method; adjust JSONL serialization
- `cmd/add.go` — Add `--estimate` flag
- `cmd/edit.go` — Add `--estimate` flag
- `cmd/show.go` — Display estimate in output
- `README.md` — Document the estimate field format and usage

### Create
- Unit tests for estimate field in `internal/task/task_test.go`
- Unit tests for estimate in store operations
- `internal/task/estimate.go` — Optional: parse helper for estimate string format (e.g., "2h" → minutes)