Phase 2: Add due-date field and command to report overdue tasks

## Phase 2 – Beta

**Goal:** Expand the feature set to cover richer metadata, improve developer experience, and harden quality via integration tests.

**Technical milestones:**
- Integration test suite covers all commands with ≥ 80% path coverage
- `tssk edit <id>` command implemented and tested
- Shell completions generated and documented

**Success metrics:**
- Integration test pass rate: 100%
- `go test ./...` runs in < 30 s on CI
- No high/critical CVEs in dependency tree

**Review point:** 2026-07-31

## Files to Modify/Create

### Modify
- `internal/task/task.go` — Add `DueDate` field (pointer `*time.Time` for optional); add overdue check helper
- `internal/store/store.go` — Update `Add()` to accept due date; add `UpdateDueDate()` method; adjust serialization
- `cmd/add.go` — Add `--due-date` flag
- `cmd/edit.go` — Add `--due-date` flag
- `cmd/list.go` — Add `--overdue` flag to filter overdue tasks

### Create
- `cmd/overdue.go` — Optional dedicated command to list overdue tasks
- Unit tests for due date handling in `internal/task/task_test.go`
- Unit tests for due date in store operations
- Integration tests for overdue reporting