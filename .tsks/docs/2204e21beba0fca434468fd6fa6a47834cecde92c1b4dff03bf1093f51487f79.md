Add a new command, 'ready', to list all tasks that have status: todo and do not depend on any other task with a status of 'todo' or 'in progress'.

## Files to Modify/Create

### Modify
- `cmd/root.go` — Register new `ready` command in `init()`
- `internal/store/store.go` — May need a helper method to resolve dependency statuses for filtering

### Create
- `cmd/ready.go` — New Cobra command for `tssk ready`; implements filtering logic:
  - Fetch all `todo` tasks
  - For each, check if any dependency has status `todo` or `in-progress`
  - Display tasks with no blocking dependencies
- `cmd/ready_test.go` — Unit tests for ready command logic
- Documentation in `README.md` for the `ready` command