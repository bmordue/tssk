Risk R-6: Single-file JSONL store causes data loss on concurrent writes. Mitigation: Atomic rename pattern already implemented; add advisory file locking in Phase 2

## Risk Details

- **Likelihood:** Low
- **Impact:** High
- **Mitigation:** Atomic rename pattern already implemented; add advisory file locking in Phase 2

## Files to Modify/Create

### Modify
- `internal/store/local.go` — Add file locking (e.g., `flock`) around read/write operations for the tasks JSONL file
- `go.mod` — Add file locking dependency (e.g., `github.com/gofrs/flock`) if not using stdlib-only approach

### Create
- `internal/store/local_test.go` — Already exists; add concurrent write tests to verify locking
- Integration test for concurrent write scenarios