- make the default config file location ./tssk/config.json
- add an option to specify the path to the config file location to all tssk commands
- update and extend tests as needed
- make sure all commands and options are fully documented in the README, and any examples are updated if needed

## Files to Modify/Create

### Modify
- `internal/store/config.go` — Change default config file path from `.tssk.json` to `./tssk/config.json`; add `--config` flag handling
- `cmd/root.go` — Add persistent `--config` flag to root command so all subcommands inherit it
- `cmd/init.go` — Update to use new config file location
- `README.md` — Document new config file location and `--config` flag
- All existing `*_test.go` files that rely on config loading — Update test setup for new paths

### Create
- `./tssk/config.json` — New default config file location (migrate from `.tssk.json`)
- `cmd/root_test.go` — Tests for the `--config` flag behavior
- Migration guide in `docs/config-migration.md`